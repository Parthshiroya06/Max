buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 23
        compileSdkVersion = 34
        targetSdkVersion = 34
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.9.24"
        RNMapboxMapsVersion = '11.0.0'
    }
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url("$rootDir/../node_modules/react-native/android") } // Ensure React Native dependencies are resolved
         maven { url 'https://api.mapbox.com/downloads/v2/releases/maven' }
        maven { url 'https://mapbox.bintray.com/mapbox' }

    }


    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
        classpath ('com.google.gms:google-services:4.3.15')
    }
}

allprojects {
    repositories {
           google()
         mavenCentral()
        
maven {
            url 'https://api.mapbox.com/downloads/v2/releases/maven'
            authentication {
                basic(BasicAuthentication)
            }
            credentials {
                // Do not change the username below.
                // This should always be `mapbox` (not your username).
                username = 'mapbox'
                // Use the secret token you stored in gradle.properties as the password
                password = project.properties['pk.eyJ1IjoiYW5raXQ4MzYiLCJhIjoiY202dnV2dTJ2MGcwazJpczg5M3FyYmhwOCJ9.OToOdFg7n-0XKb6tA8BWdw'] ?: ""
            }
        }


    }
}


apply plugin: "com.facebook.react.rootproject"
